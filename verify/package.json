{
  "name": "altarwed-verify",
  "private": true,
  "type": "module",
  "description": "Local /verify harness: API seed + Playwright browser checks against the Testcontainers backend.",
  "scripts": {
    "seed": "node seed.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
